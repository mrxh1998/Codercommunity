<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coder.community.dao.DiscussPostMapper">
    <sql id="selectFields">
        id,user_id,title,type,status,create_time,comment_count,score,content
    </sql>
    <sql id="insertFields">
        user_id,title,type,status,create_time,comment_count,score,content
    </sql>
    <select id="selectDiscussPosts" resultType="DiscussPost">
        select <include refid="selectFields"></include>
        from discuss_post
        where status != 2
        <if test="userId != 0">
            and user_id = #{userId}
        </if>
        order by type DESC,create_time DESC
        limit #{offset},#{limit}
    </select>
    <select id="selectDiscussPostsRows" resultType="int">
        select count(id)
        from discuss_post
        where status != 2
        <if test="userId != 0">
            and user_id = #{userId}
        </if>
    </select>
    <insert id="insertDiscussPost" parameterType="DiscussPost">
        insert into discuss_post(<include refid="insertFields"></include>)
        value(#{userId},#{title},#{type},#{status},#{createTime},#{commentCount},#{score},#{content})
    </insert>
    <select id="selectById" resultType="DiscussPost">
        select <include refid="selectFields"></include> from discuss_post
        where id = #{id}
    </select>
    <update id="updateCommentCount">
        update discuss_post
        set comment_count = #{count}
        where id = #{id}
    </update>
</mapper>